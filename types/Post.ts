export type Post = {
    postId: String,
    parentId: String,
    reply: String,
    userName: String,
    createdAt: String,
    updatedAt: String,
    children: String[]
}


export type PostT = {
    postId: String,
    parentId: String,
    reply: String,
    userName: String,
    createdAt: String,
    updatedAt: String,
    children: String[]
}
